# Local dev points at the local server. In Vercel/Netlify set this to your VM URL,
# e.g. https://api.yourdomain.com (Socket.IO upgrades to wss:// automatically).
VITE_SERVER_URL=http://localhost:3000
